// Template that wraps all page content
// Animation removed to prevent mobile rendering issues
export default function Template({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}

